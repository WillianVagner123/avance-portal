name: Google Calendar Sync

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      SYNC_URL: ${{ secrets.SYNC_URL }}
      SYNC_SECRET: ${{ secrets.SYNC_SECRET }}
    steps:
      - name: Call Google Sync Endpoint
        run: |
          if [ -z "$SYNC_URL" ]; then
            echo "ERROR: SYNC_URL is empty. Configure repo secret SYNC_URL."
            exit 1
          fi

          echo "Calling: $SYNC_URL"
          curl -fsS -X POST "$SYNC_URL" \
            -H "x-sync-secret: $SYNC_SECRET"

# The trailing digits above were accidentally committed and break the YAML file.
# They have been removed so that the workflow is valid. The workflow calls
# your Cloud Run service’s Google Calendar sync endpoint on a five‑minute
# schedule. To enable this workflow, configure the following repository
# secrets on GitHub:
#
#   SYNC_URL    – full URL of the Cloud Run sync endpoint (for example
#                 https://your-service-url/run/api/google/sync)
#   SYNC_SECRET – the same value used for the SYNC_SECRET environment
#                 variable on your Cloud Run deployment.
